{
  "name": "hub-launcher",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "docker-build": "docker build -t hub .",
    "docker": "bash -c 'PORT=${1:-1997} && docker run -p 127.0.0.1:$PORT:1997 --restart=always hub' --"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "hub-service": "v57/hub-service"
  }
}
